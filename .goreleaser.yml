# .goreleaser.yml
version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: discord-cleanup
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}}
    binary: discord-cleanup

dockers:
  - image_templates:
      - "kwv4/discord-cleanup:{{ .Version }}-amd64"
    use: docker
    dockerfile: Dockerfile
    build_flag_templates:
      - "--platform=linux/amd64"
      - "--label=org.opencontainers.image.version={{.Version}}"
    goos: linux
    goarch: amd64

  - image_templates:
      - "kwv4/discord-cleanup:{{ .Version }}-arm64"
    use: docker
    dockerfile: Dockerfile
    build_flag_templates:
      - "--platform=linux/arm64"
      - "--label=org.opencontainers.image.version={{.Version}}"
    goos: linux
    goarch: arm64

docker_manifests:
  - name_template: "kwv4/discord-cleanup:{{ .Version }}"
    image_templates:
      - "kwv4/discord-cleanup:{{ .Version }}-amd64"
      - "kwv4/discord-cleanup:{{ .Version }}-arm64"
  - name_template: "kwv4/discord-cleanup:latest"
    image_templates:
      - "kwv4/discord-cleanup:{{ .Version }}-amd64"
      - "kwv4/discord-cleanup:{{ .Version }}-arm64"

archives:
  - format: tar.gz
    # use zip for windows archives
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: 'checksums.txt'

snapshot:
  name_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
